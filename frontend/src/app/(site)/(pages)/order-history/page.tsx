type PageProps = {
    
    children?: React.ReactNode
}

function OrderHistoryPage({ children }: PageProps) {
    return (
        <div>
            
        </div>
    )
}

export default OrderHistoryPage
