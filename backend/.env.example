DB_URL=
DB_USERNAME=postgres
DB_PASSWORD=yourpassword

# Generate yourself: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Required: exactly 64 hex characters. Never commit your real key.
JWT_SECRET=
