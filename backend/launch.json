{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "Run PandaStore",
      "request": "launch",
      "mainClass": "project.project.ProjectApplication",
      "cwd": "${workspaceFolder}/backend",
      "envFile": "${workspaceFolder}/backend/.env"
    }
  ]
}
